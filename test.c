#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#include <stdlib.h>
#define BUFFSIZE 4096
int main()
{
    /*char buf[BUFFSIZE]="123456";
    char tmp[BUFFSIZE];
    FILE *file = fopen("./test.txt","r+");
    fprintf(file,"%s",buf);
    fflush(NULL);
    fgets(tmp,BUFFSIZE,file);
    printf("%s\n",tmp);*/
    return 0;
}

